---
title: "API Request"
description: "Hier erfährst du, wie du API Requests in der Nachverarbeitung von Anrufen bei fonio nutzen kannst."
---

In diesem Beitrag lernst du Schritt für Schritt, wie du mit fonio nach einem Anruf automatisch einen API Request an dein eigenes System senden kannst. So kannst du z. B. CRM-Workflows starten, ein Ticket anlegen oder Daten in ein internes Tool schreiben.

## So funktioniert es

<Steps>
  <Step title="API Request Nachverarbeitung erstellen">
    Öffne beim Agenten die Nachverarbeitung und wähle `API Request` aus.

    Lege dann fest:

    1. `Methode:` z. B. `POST`
    2. `URL:` dein Ziel-Endpoint
    3. `Headers:` optional, z. B. `Authorization`
  </Step>

  <Step title="Request-URL dynamisch aufbauen (optional)">
    Du kannst Variablen in der URL verwenden. fonio ersetzt diese vor dem Versand automatisch.

    Beispiel:

    ```text
    https://example.com/hooks/{{leadId}}?source={{context.source}}
    ```

    Dabei können auch Werte aus `context` verwendet werden.
  </Step>

  <Step title="Payload empfangen">
    Dein Endpoint erhält nach dem Anruf ein JSON-Payload mit den wichtigsten Anrufdaten.

    Typische Felder sind:

    - `id`
    - `summary`
    - `transcript`
    - `formattedTranscript`
    - `fromNumber`
    - `toNumber`
    - `direction`
    - `duration`
    - `disconnectReason`
    - `startTimestamp`
    - `endTimestamp`
    - `audioLink`
    - `context`
    - `extractionData`
    - `sipHeaders`
  </Step>

  <Step title="Speichern & Testen">
    Speichere die Nachverarbeitung und teste sie mit einem Testanruf.

    Der API Request wird nach dem Anruf automatisch ausgelöst.
  </Step>
</Steps>

## Beispiel-Payload (JSON)

```json
{
  "id": "8f6c8a7a-2d6e-4df0-9d88-2a1c9f3b8e41",
  "summary": "Der Anrufer möchte ein Angebot per E-Mail erhalten.",
  "transcript": [
    {
      "role": "caller",
      "text": "Guten Tag, ich hätte gerne ein Angebot.",
      "timestamp": "2026-03-10T09:12:10.000Z"
    },
    {
      "role": "assistant",
      "text": "Gerne, ich nehme die Details auf.",
      "timestamp": "2026-03-10T09:12:15.000Z"
    }
  ],
  "formattedTranscript": "Anrufer: Guten Tag, ich hätte gerne ein Angebot.\nAssistent: Gerne, ich nehme die Details auf.",
  "fromNumber": "+436601112233",
  "toNumber": "+43720072767",
  "direction": "inbound",
  "duration": 186,
  "disconnectReason": "caller_hangup",
  "startTimestamp": "2026-03-10T09:12:10.000Z",
  "endTimestamp": "2026-03-10T09:15:16.000Z",
  "audioLink": "https://storage.example.com/recordings/call-8f6c8a7a.mp3",
  "context": {}
}
```

<Tip>
  **Wichtig:** Sichere produktive Endpoints immer sauber ab (z. B. Auth Header, IP-Filter, Signaturprüfung).
</Tip>

Für Fragen oder Unterstützung steht dir unser Team unter [**support@fonio.ai**](mailto:support@fonio.ai) zur Verfügung.
